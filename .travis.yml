language: c

before_install:
    - bash install.sh

script:
    - bash build.sh

notifications:
    on_failure: change
    on_success: change

cache: 
    directories:
        - cache
    apt: true

sudo: required

services:
    - docker
